# CARACOL Pipeline - Python Dependencies
# Windows / Linux

# Core
python-dotenv>=1.0.0
requests>=2.31.0
urllib3>=2.0.0

# Neo4j
neo4j>=5.0.0

# LLM
openai>=1.0.0

# Obsidian / Markdown
python-frontmatter>=1.0.0
pyyaml>=6.0

# Data processing
pydantic>=2.0.0

# Vector store (optional)
# chromadb>=0.4.0

# Attachment parsing — Motor Padrão (rápido, leve)
markitdown>=0.1.0

# PDF conversion com alta fidelidade — Motor Docling (CPU-only)
docling>=2.0.0
# NOTA: PyTorch CPU-only — instalar separado:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# Arquivos compactados (ZIP nativo, TAR nativo)
py7zr>=0.20.0
rarfile>=4.0

# MS Project files (requer Java JRE 8+)
# mpxj>=13.0.0
# jpype1>=1.5.0

# LangGraph (only for query engine)
langgraph>=0.0.1
langchain-core>=0.1.0

# RabbitMQ
pika>=1.3.0
